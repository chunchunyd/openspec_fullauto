# 任务拆解：Auth Step 06 设备会话管理与退出登录

## 1. 实施前门禁

- [x] 从最新 `dev` 切出 `feat/auth-step-06-device-sessions-and-logout` 并确认主项目工作树边界
- [x] 确认 `auth-step-01` 与 `auth-step-04` 已完成或其会话模型 / 登录态边界已可复用
- [x] 阅读当前 `auth` spec 与本 change 设计说明

## 2. 契约与治理边界

- [x] 为 `auth` capability 建立本次 step 的 spec delta
- [x] 定义设备会话列表返回结构与移除 / 退出接口 contract
- [x] 执行 `openapi-export` 并确认共享契约产物更新

## 3. 会话治理实现

- [x] 实现查看设备会话列表
- [x] 实现移除指定设备会话
- [x] 实现当前设备退出登录
- [x] 确保被撤销会话无法继续通过 refresh token 维持登录态

## 4. 验证与测试

- [x] 为查看列表、移除会话、退出当前设备补测试
- [x] 为会话归属校验补测试
- [x] 验证被移除设备后续 refresh 会被明确拒绝

### 手动验收步骤

1. 登录后查看设备会话列表，应返回当前账号可管理的设备信息
2. 移除指定设备后，该设备的 refresh token 应失效
3. 当前设备执行退出登录后，应无法继续使用原登录态访问受保护接口或刷新会话
