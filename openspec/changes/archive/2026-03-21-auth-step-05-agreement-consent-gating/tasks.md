# 任务拆解：Auth Step 05 协议确认与准入阻塞

## 1. 实施前门禁

- [x] 从最新 `dev` 切出 `feat/auth-step-05-agreement-consent-gating` 并确认主项目工作树边界
- [x] 确认 `auth-step-01` 与 `auth-step-04` 已完成或其模型 / 登录态边界已可复用
- [x] 阅读 当前 `auth` spec 与本 change 设计说明

## 2. 契约与准入语义

- [x] 为 `auth` capability 建立本次 step 的 spec delta
- [x] 明确”认证成功”与”允许进入完整产品能力”之间的边界
- [x] 如新增确认接口或 gating 返回结构，执行 `openapi-export`

## 3. 协议确认与 gating 实现

- [x] 实现必要协议 / AI 告知确认状态判定
- [x] 实现确认写入接口或等价服务
- [x] 实现首次未确认用户的准入阻塞逻辑
- [x] 实现已确认用户的免重复阻塞逻辑

## 4. 验证与测试

- [x] 为首次未确认用户补 gating 测试
- [x] 为确认写入成功 / 失败补测试
- [x] 为已确认用户后续登录免阻塞补测试

### 手动验收步骤

1. 首次登录但未确认协议的用户，应得到受控阻塞结果
2. 完成必要协议确认后，应能进入完整产品能力
3. 已确认用户再次登录时，不应被重复阻塞
